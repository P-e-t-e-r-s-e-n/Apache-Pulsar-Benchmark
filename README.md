The given bashscript is for messags sizes of 1024 bytes. Messge sizes can be changed at the "MSG_SIZE" Part.

The log naming follows the pattern: 

`<place of changed setting>_<CPU ressources>_<RAM ressources>_<run number>_<consumer or porducer side>`
